[project]
name = "Cool Note API"
version = "1.0.11"
description = "A simple template for Cool API applications."

readme = "README.md"
requires-python = ">=3.11"

[project.dependencies]
dependencies = "requirements.txt"

[project.optional-dependencies]
dev-requirements = {file = "dev-requirements.txt"}
